#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	char n = '@';
	char n = '@';
	for (n == '@'; n <= 'I'; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
